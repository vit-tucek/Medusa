IMPORTANT DISCLAIMER

All code changes and additions in this repository were generated by
GPT-5.3-Codex.

There are no guarantees of correctness, completeness, safety, or fitness
for any purpose. Use at your own risk and validate results independently.

INSTALLING MEDUSA

Directory layout:
- `src/`: all C/C++ source and header files
- `build/obj/`: object files produced by `make`
- repository root: executables, documentation, scripts, and generated output dirs

Build:
- `make all`

Clean binaries and object files:
- `make clean`

Generate example graphics (PDF):
- `./generate_graphics.sh`
- or `./generate_graphics.sh <output_dir>`

Windows + VS Code (MSVC):
- Open the folder in VS Code from a "Developer PowerShell for VS" terminal.
- Run task: `Build All (MSVC)` (Ctrl+Shift+B).
- Binaries are generated in repository root as `.exe` files.
- Equivalent terminal commands:
- `cmake -S . -B build/vscode-msvc -G "Visual Studio 17 2022" -A x64`
- `cmake --build build/vscode-msvc --config Release`
- Optional sample output generation:
- `powershell -ExecutionPolicy Bypass -File .\generate_graphics.ps1`
- or `powershell -ExecutionPolicy Bypass -File .\generate_graphics.ps1 -OutputDir output`

Python port (NumPy + Matplotlib):
- Module: `python/medusa_modern.py`
- Full lifting engine (mate_interact port): `python/medusa_full.py`
- CLI generator: `python/generate_graphics.py` (run from repo root)
- Example:
- `python3 python/generate_graphics.py --param-file param.txt --output-dir output_py --dpi 96`
- Generate parameters directly from angles with Python full port:
- `python3 python/mate_interact_py.py --p1 1 --q1 7 --p2 3 --q2 7 --iterations 35 --output output_py/param_py.txt`
- Add `--stop-on-failure` for strict mode (default continues like `mate_interact` loop).
- Jupyter showcase notebook:
- `notebooks/medusa_python_showcase.ipynb`
- Launch with:
- `jupyter notebook notebooks/medusa_python_showcase.ipynb`


Programs were written by Christian Henriksen, David Farris, and Kuan Ju
Liu at Cornell University in Summer 1998.
